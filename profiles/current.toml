name = "current"
notes = """
The mix config.toml has been running (pool fractals_1). This is the no-op
baseline: loading it and starting a run reproduces current behaviour exactly.

Image-embedding novelty is on at 0.5 here and nowhere else, because
fractals_1 is the only pool with a trained novelty head and a
.novelty_cache.npz. Turning it on for another pool does nothing until that
pool has one.

min_ensemble is stated explicitly. It is absent from every config*.toml and
has therefore been silently running at its 4.6 code default — it is the
single most consequential save gate, so it should be visible.
"""

# ── selection ──
entropy_weight           = 1.0
novelty_weight           = 0.60
formula_diversity_weight = 0.60
ood_weight               = 0.25
angle_structure_weight   = 0.0
recursion_pred_weight    = 0.0     # inert for DAG genomes — see the knob's help text
clip_pred_weight         = 0.0     # inert for DAG genomes
complexity_penalty       = 0.012
duplicate_penalty_weight = 0.50

# ── saved fitness + seeding ──
pref_weight              = 0.4
musiq_weight             = 0.25
img_novelty_weight       = 0.5
seed_pref_weight         = 3.0
seed_aesthetic_weight    = 1.0
seed_laion_weight        = 0.15
self_replication_weight  = 0.35
fractal_recursion_weight = 0.35

# ── save gate ──
min_ensemble          = 4.6
min_entropy_prefilter = 0.38
max_entropy_prefilter = 0.70
min_save_distance     = 0.05
min_beauty            = 0.35
min_musiq             = 58.0
min_pref              = 0.55
